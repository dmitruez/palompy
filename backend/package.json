{
  "name": "backend",
  "version": "1.0.0",
  "description": "Palompy demo backend without external npm dependencies",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/src/index.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
